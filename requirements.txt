fastapi>=0.115.0
uvicorn[standard]>=0.30.0
transformers>=4.52.0,<5.0.0
torch==2.8.0
torchvision==0.22.1
accelerate>=0.34.0
bitsandbytes>=0.46.0
pillow>=10.4.0
opencv-python-headless>=4.10.0.84
python-multipart>=0.0.9
qwen-vl-utils>=0.0.9
requests>=2.32.0

# Flash Attention 2 is optional and can be enabled in the Docker build with:
#   docker compose build --build-arg INSTALL_FLASH_ATTN=1 vlm-api-gpu
# Install separately after confirming CUDA toolkit version:
#   pip install flash-attn --no-build-isolation
# Pre-built wheels (faster): https://github.com/Dao-AILab/flash-attention/releases
